
import IconRadioDefault from './radio-default.png'
import IconRadioChecked from './radio-checked.png'
import IconChecked from './checkbox-checked.png'
import { StaticImageData } from 'next/image'

export type Icons = {
  IconRadioDefault: StaticImageData,
  IconRadioChecked: StaticImageData,
  IconChecked: StaticImageData
}

const icons: Icons = {
  IconRadioDefault,
  IconRadioChecked,
  IconChecked
}

export default icons
